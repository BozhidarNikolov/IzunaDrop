@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<IzunaDrop.Data.Models.Character>;
@{
}
<ul>

    @if (Model.Count() == 0)
    {
        <div>
            No characters yet
        </div>
    }
    else
    {
        @foreach (var character in Model)
        {
            <div>
                @character.Name
            </div>
        }
    }
</ul>
